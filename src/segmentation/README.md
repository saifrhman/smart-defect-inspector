# Segmentation (U-Net)

This module will handle pixel-level defect segmentation.

Planned scripts:
- `train_unet.py`   Train a U-Net style model on mask labels
- `infer_unet.py`   Run inference and save predicted masks + overlays

For now we scaffold the module and create a stub runner.
